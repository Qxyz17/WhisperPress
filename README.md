WhisperPress
============
本地一键启动端到端加密聊天（64位ID）

1. 安装 PHP（≥7.4）
2. 进入本目录，运行  php -S localhost:8080
3. 浏览器打开 http://localhost:8080
   - 每次刷新自动生成 64 位唯一 ID，点击可复制
   - 把 ID 发给对方，对方在地址栏 # 后粘贴即可进入同一房间
4. 服务器仅缓存密文，明文永不落地。
